import 'package:flutter/material.dart';

class YamulWip extends StatelessWidget {
  const YamulWip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('WIP'),
    );
  }
}
